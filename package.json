{
  "name": "blitz",
  "version": "1.0.0",
  "description": "Blitz – a fast, minimal, monochrome study app powered by Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "preview": "npx serve public"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4"
  },
  "devDependencies": {
    "wrangler": "^3.91.0"
  }
}
